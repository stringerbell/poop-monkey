{
  "name": "poop-monkey",
  "version": "1.0.0",
  "private": true,
  "description": "First-person zoo-escape game. Eat, escape, and pelt the guards.",
  "type": "module",
  "scripts": {
    "start": "python3 -m http.server 8123",
    "test": "node --test 'test/*.test.mjs'"
  },
  "devDependencies": {
    "three": "^0.169.0"
  }
}
